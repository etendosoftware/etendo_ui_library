import { Meta } from '@storybook/blocks';
import Comments from './assets/comments.svg';
import StackAlt from './assets/stackalt.svg';
import ButtonIcon from './assets/button.svg';
import InputIcon from './assets/input.svg';
import NavbarIcon from './assets/navbar.svg';
import PaginationIcon from './assets/pagination.svg';
import TabIcon from './assets/tab.svg';
import TableIcon from './assets/table.svg';
import CardIcon from './assets/card.svg';
import CopyIcon from './assets/copy.svg';

import {
  INITIAL_90,
  NEUTRAL_70,
  NEUTRAL_80,
  NEUTRAL_90,
  PRIMARY_20,
  PRIMARY_100,
  SUCCESS_10,
  SUCCESS_100,
  QUATERNARY_10,
  QUATERNARY_70,
  QUATERNARY_100,
} from './styles/colors';

<Meta title="How To Use/Steps" />

<style>
  {`
    .inter {
      font-family: 'Inter', 'sans-serif';
    }

    .description {
      font-size: 1rem;
    }

    .keyword {
      font-weight: bold;
    }

    .subheading {
      font-weight: 700;
      font-size: 13px;
      letter-spacing: 5px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
      color: ${NEUTRAL_70};
    }

    .link-list {
      display: grid;
      grid-template-columns: 1fr;
      row-gap: 10px;
    }

    @media (min-width: 620px) {
      .link-list {
        row-gap: 20px;
        column-gap: 20px;
      }
    }

    @media all and (-ms-high-contrast:none) {
      .link-list {
          display: -ms-grid;
          -ms-grid-columns: 1fr 1fr;
          -ms-grid-rows: 1fr 1fr;
      }
    }

    .link-item {
      display: block;
      padding: 20px;
      border-radius: 5px;
      transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
      display: flex;
      align-items: flex-start;
    }

    .link-item:hover {
      transform: translate3d(0, -3px, 0);
      box-shadow: ${NEUTRAL_80} 0 3px 10px 0;
    }

    .link-item:active {
      transform: translate3d(0, 0, 0);
    }

    .link-item strong {
      font-weight: 700;
      display: block;
      margin-bottom: 2px;
    }

    .link-item img {
      height: 40px;
      width: 40px;
      margin-right: 15px;
      flex: none;
    }

    .link-item span,
    .link-item p {
      margin: 0;
      font-size: 14px;
      line-height: 20px;
    }

    .tip {
      display: inline-block;
      border-radius: 1em;
      font-size: 11px;
      line-height: 12px;
      font-weight: 700;
      background: ${SUCCESS_10};
      padding: 4px 12px;
      margin-right: 10px;
      vertical-align: top;
    }

    .tip-wrapper {
      font-size: 13px;
      line-height: 20px;
      margin-top: 40px;
      margin-bottom: 40px;
    }

    .tip-wrapper code {
      font-size: 12px;
      display: inline-block;
    }

    .step-one {
      padding: 5px 10px;
      border-radius: 5px;
    }

    .step-two {
      padding: 5px 10px;
      border-radius: 5px;
    }

      .link-item {
      display: block;
      padding: 20px;
      border: 1px solid ${NEUTRAL_80};
      border-radius: 5px;
      transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
      color: ${PRIMARY_20};
      display: flex;
      align-items: flex-start;
    }

    .link-item:hover {
      border-color: ${QUATERNARY_100};
      transform: translate3d(0, -3px, 0);
      box-shadow: var(--transparent-black) 0 3px 10px 0;
    }

    .link-item:active {
      border-color: ${INITIAL_90};
      transform: translate3d(0, 0, 0);
    }

    .code-container {
      background-color: ${NEUTRAL_90};
      padding: 0.25rem 1.25rem;
      border-radius: 4px;
      color: white;
      display: flex;
      align-items: center;
    }

    .code-container img {
      width: 1rem;
      cursor: pointer;
    }

    .button-code {
      background-color: ${QUATERNARY_10};
      border: 1px solid ${PRIMARY_100};
      padding: 0.5rem 0.75rem;
      border-radius: 4px;
      cursor: pointer;
    }

    .button-code:hover {
      background-color: ${QUATERNARY_10};
    }
  `}
</style>

<h1 className="inter">How to use Storybook Playground</h1>

<p className="description inter">
  To begin using this playground, simply follow the steps outlined in the
  provided workflow.
</p>
{/* 
<div className="subheading inter">Workflow </div> */}

<div className="link-list">
  <div className="link-item">
    <div>
      <strong className="step-title inter">🚀 Step 1</strong>
      <br />
      <span className="inter">Install <span className="keyword">all dependencies</span> using Yarn in the root path:</span>
      <br />
      <br />

      <div className="code-container">
        <img src={CopyIcon} onClick={() => navigator.clipboard.writeText(document.getElementById('code1').innerText)} />
        <pre><code id="code1">yarn </code></pre>
      </div>
    </div>

  </div>
  <div className="link-item">
    <div>
      <strong>📦 Step 2</strong>
      <br />
      <span className="inter">Open a terminal in the base path of <span className="keyword">Etendo UI Library</span> and excecute this command:</span>
      <br />
      <br />

      <div className="code-container">
          <img src={CopyIcon} onClick={() => navigator.clipboard.writeText(document.getElementById('native-code').innerText)} />
          <pre><code id="native-code">yarn watch</code></pre>
      </div>

  </div>
</div>
<div className="link-item">
<div>
    <strong>🚀 Step 3</strong>
    <br />
    <span className="inter">Open the project and modify the <span className="keyword">Main story</span> in the <span className="keyword">stories</span> folder. In this case is the <span className="keyword">Button Container</span>.</span>
    <br />
    <span className="inter">Then you are ready to excecute the project and visualize the changes using the following command: </span>
    <br />
    <br />

    <div className="code-container">
        <img src={CopyIcon} onClick={() => navigator.clipboard.writeText(document.getElementById('native-code').innerText)} />
        <pre><code id="native-code">yarn storybook</code></pre>
    </div>

  </div>

</div>

</div>

<p className="inter">
  > <span className="keyword">Note:</span> Once you excecuted the command, you
  will be able to see the changes in the browser. Storybook has hotreload, so
  you don't need to restart the server to see the changes.
</p>

<p className="inter">
  > <span className="keyword">Important:</span> Once you excecuted the command,
  you will be able to see the changes in the browser. Storybook has hotreload,
  so you don't need to restart the server to see the changes.
</p>
